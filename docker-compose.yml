version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - EMAIL=likhithakadavakolla@gmail.com
      - PASSWORD=pnmjaxrnxouwckmj
    volumes:
      - ./backend:/app/backend
      - ./frontend/data:/app/frontend/data

  frontend:
    image: node:18-alpine
    working_dir: /app
    ports:
      - "3000:3000"
    volumes:
      - ./frontend-react:/app
    environment:
      - REACT_APP_API_URL=http://localhost:8000
      - BROWSER=none
    command: sh -c "npm install && npm start"
    depends_on:
      - backend
